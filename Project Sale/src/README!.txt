và đây là bộ xương sống của code, tất cả các phần đã được chia ra đúng theo một mô hình MVC cụ thể gồm
3 phần chính Model, View, Controller ngoài ra còn các thành phần khác đi kèm nhằm bổ trợ cho code có 
thể dùng được ổn định, chi tiết hơn trong từng file
