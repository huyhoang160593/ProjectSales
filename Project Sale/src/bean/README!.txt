Sử dụng java bean làm một class dùng để chứa lớp Danh mục, góp một phần không nhỏ trong việc tạo nên
giao diện custom(chạy thử sẽ thấy) dùng để chứa các thuộc tính Panel(khung chứa danh mục), Label(tên
danh mục) và kind(kiểu danh mục) 